{% extends 'base.html.twig' %}


{% block body %}

<div class="container">
  <h2>Zones </h2>

  <a href="{{ path('ho_create_zone') }}" class="btn btn-default btn-sm" >
    <span class="glyphicon glyphicon-globe"></span> Ajouter</a><br/><br/>
  <input id="myInput"  placeholder="Recherche.." > <br/><br/>
           
  <table class="table table-striped">
    <thead>
      <tr>
      	<th> Id  </th>
        <th> zone </th>
        <th> Mettre à jour </th>
        <th> Supprimer </th>
      </tr>
    </thead>
    <tbody id="myTable">
    {% for zone in zones %}
      <tr>
        <td> {{ zone.id }} </td>
        <td> {{ zone.name }} </td>
        <td> <a href="{{ path('ho_update_zone',{'id': zone.id }) }}"> lien </a></td>
        <td> <a href="{{ path('ho_delete_zone',{'id': zone.id }) }}"> lien </a> </td>
      </tr>
    {% endfor %}
      
    </tbody>
  </table>
</div>
 <br/><br/><br/><br/><br/>
<script>
$(document).ready(function(){
	$("#myInput").on("keyup", function(){
	var value = $(this).val().toLowerCase();

	$("#myTable tr").each(function(){
	 $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
	});
	});
});

</script>



{% endblock %}