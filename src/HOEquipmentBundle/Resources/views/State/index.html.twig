{% extends 'base.html.twig' %}


{% block body %}

<div class="container">
  <h2> Etat d'équipement </h2>

  <a href="{{ path('ho_create_state') }}" class="btn btn-default btn-sm" >
    <span class="glyphicon glyphicon-bell"></span> Ajouter</a><br/><br/>
  <input id="myInput"  placeholder="Recherche.." > <br/><br/>
           
  <table class="table table-striped">
    <thead>
      <tr>
      	<th> Id  </th>
        <th> Etat </th>
        <th> Mettre à jour </th>
        <th> Supprimer </th>
      </tr>
    </thead>
    <tbody id="myTable">
    {% for state in states %}
      <tr>
        <td> {{ state.id }} </td>
        <td> {{ state.state }} </td>
        <td> <a href="{{ path('ho_update_state',{'id': state.id }) }}"> lien </a></td>
        <td> <a href="{{ path('ho_delete_state',{'id': state.id }) }}"> lien </a> </td>
      </tr>
    {% endfor %}
      
    </tbody>
  </table>
</div>
 <br/><br/><br/><br/><br/>
<script>
$(document).ready(function(){
	$("#myInput").on("keyup", function(){
	var value = $(this).val().toLowerCase();

	$("#myTable tr").each(function(){
	 $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
	});
	});
});

</script>



{% endblock %}